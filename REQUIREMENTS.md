# 競馬予想ロジック・システム構築の要件定義書

これからPythonを使った競馬の予想ロジックおよびバックテストシステムの開発を行います。
全体の前提条件と設計思想を以下に示します。

---

## 1. 開発環境・スタンス

- **OS**: Windows
- **使用言語・環境**: Python、SQLite（データ保存用）
  - SQLiteはPython標準ライブラリに含まれるため、追加のDB契約・インストールは不要
  - データは `.db` ファイル1つで管理する
- **スクレイピング方針**:
  - データソースはnetkeiba等の無料アクセス可能なサイトを想定
  - netkeibaはJavaScriptレンダリングが必要なページが多いため、**SeleniumまたはPlaywright**を使用する
  - スクレイピング前にrobots.txtおよび利用規約を必ず確認すること
  - サイトに負荷をかけないよう、リクエスト間に適切なスリープ処理（例：2〜5秒）を入れること
- **参加スタンス**:
  - ガチガチの投資目的というよりは「毎週競馬を楽しく観戦する（エンタメ性）」を重視する
  - **競馬開催日に**1日3レース程度に厳選して参加する

---

## 2. レース選定（フィルタリング）の考え方

条件を厳しくしすぎて「今週は参加できるレースがゼロ」になることを防ぐため、以下の2段階アプローチを採用する。

### ステップA：除外フィルター（一律除外）
以下のレースは予測精度が低い・ロジックが異なるため、最初から除外する。
- **新馬戦**：出走履歴データが極端に少ない
- **障害レース**：平地レースとは予測ロジックが根本的に異なる

### ステップB：スコアリングによる選定
除外後に残ったレースすべてに対して「予測の自信度スコア」を計算し、**その開催日の全レースの中でスコア上位3レースに自動参加**する。
→ 開催日であれば毎回確実に3レースを楽しめる仕組みになる。

---

## 3. 開発ロードマップ（予定）

### ステップ1：データ収集環境の構築 ← **まずここから始める**
- netkeiba等から過去レースデータをスクレイピングするスクリプトの作成
- 取得したデータをSQLiteに保存する
- **目的**：バックテスト検証用の過去データと、今後の予想に使うデータを同一DBに蓄積する

### ステップ2：特徴量エンジニアリング（ロジックの肝）
- 過去データから「勝敗に直結する指標」を計算・加工する
- 想定指標例：前走着順、タイム指数、コース適性、距離適性、騎手成績、馬場状態など

### ステップ3：買い目・点数の決定ロジック
- 馬の評価スコアをもとに、券種（馬連・3連複など）と購入点数を決定するアルゴリズムを構築する

### ステップ4：バックテスト（答え合わせ）
- 過去データを使って、作成したロジックの的中率・回収率をシミュレーションする

---

## 4. データ設計方針（SQLite）

### 想定テーブル構成（例）
| テーブル名 | 内容 |
|---|---|
| `races` | レース基本情報（開催日・競馬場・距離・馬場など） |
| `horses` | 馬の基本情報（馬名・性別・生年月日など） |
| `entries` | 出走情報（レースと馬の紐付け・騎手・斤量・オッズなど） |
| `results` | レース結果（着順・タイムなど） |
| `predictions` | システムが出力した予測スコアと買い目（ログ用） |

### 規模感
- netkeibaの過去10年分のデータを全保存しても数百MB〜1GB程度に収まる見込み
- SQLiteは数GBまで問題なく動作するため、容量面での懸念はない

---

## 5. 注意事項・制約

- スクレイピングは**利用規約・robots.txtを遵守**した範囲で行うこと
- netkeibaは会員登録が必要なページがあるため、取得対象は**ログイン不要で閲覧できるページ**に限定する
- 本システムはあくまで**個人の趣味・学習目的**であること
